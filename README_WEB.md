# Web 界面使用指南

## 🚀 快速开始

### 1. 安装依赖

```bash
pip install -r requirements.txt
```

### 2. 启动服务器

```bash
python app.py
```

服务器将在 `http://localhost:5000` 启动。

### 3. 打开浏览器

访问 `http://localhost:5000` 即可使用Web界面。

---

## 📋 功能说明

### 输入参数

#### 出生档案
- **出生年份**: 1949-2020
- **地区**: 城市/郊区/农村/大都市
- **家庭阶层**: 0-1 (滑动条)
- **父母教育水平**: 0-1
- **家庭稳定性**: 0-1
- **遗传健康**: 0-1
- **认知潜力**: 0-1

#### 性格特质
- **开放性**: 0-1
- **尽责性**: 0-1
- **风险偏好**: 0-1
- **社交驱动**: 0-1
- **韧性**: 0-1

#### 模拟设置
- **国家**: 目前支持中国
- **城市**: 北京/上海/深圳/广州
- **最大年龄**: 1-100岁

### 输出结果

#### 人生总结
- 自动生成的人生叙事总结

#### 关键指标
- 最终年龄
- 最终财富
- 最终收入
- 教育水平
- 健康状况
- 压力水平

#### 人生事件
- 模拟过程中发生的重大事件列表

#### 环境信息
- 国家/时代信息
- 城市信息
- 经济周期
- 技术水平

---

## 🔧 开发说明

### 项目结构

```
AnotherWorld/
├── app.py                 # Flask应用主文件
├── templates/
│   └── index.html        # Web界面HTML
├── static/
│   ├── style.css        # 样式文件
│   └── app.js           # 前端JavaScript
└── ...
```

### API端点

#### POST `/api/simulate`
运行人生模拟

**请求体**:
```json
{
  "birth_year": 1990,
  "region": "Urban",
  "family_class": 0.5,
  "parents_education": 0.5,
  "family_stability": 0.5,
  "genetic_health": 0.7,
  "cognitive_potential": 0.6,
  "openness": 0.5,
  "conscientiousness": 0.5,
  "risk_preference": 0.5,
  "social_drive": 0.5,
  "resilience": 0.5,
  "country": "China",
  "city": "beijing",
  "max_age": 80
}
```

**响应**:
```json
{
  "success": true,
  "person": {...},
  "base_world": {...},
  "country": {...},
  "city": {...},
  "events": [...],
  "summary": {...},
  "trajectory": {...}
}
```

#### GET `/api/cities?country=China`
获取可用城市列表

#### GET `/api/health`
健康检查端点

---

## 🎨 自定义

### 修改样式

编辑 `static/style.css` 文件。

### 修改界面

编辑 `templates/index.html` 文件。

### 修改逻辑

编辑 `static/app.js` 文件。

### 扩展API

在 `app.py` 中添加新的路由。

---

## 🐛 故障排除

### 端口被占用

修改 `app.py` 中的端口号：
```python
app.run(debug=True, host='0.0.0.0', port=5001)  # 改为其他端口
```

### 模块未找到

确保在项目根目录运行，并已安装所有依赖：
```bash
pip install -r requirements.txt
```

### CORS错误

如果从其他域名访问，确保 `flask-cors` 已安装并正确配置。

---

## 📝 注意事项

1. **性能**: 模拟可能需要几秒钟，请耐心等待
2. **浏览器兼容性**: 建议使用现代浏览器（Chrome, Firefox, Edge）
3. **数据**: 所有计算在服务器端完成，不会保存用户数据
4. **免责声明**: 本系统仅用于思想实验和叙事生成，不提供现实人生建议

---

**享受模拟！** 🎉

